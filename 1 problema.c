#include <stdio.h>

int main ()
{
    float quilo;
    float qtd;
    float soma;

    scanf("%f", &quilo);
    scanf("%f", &qtd);
    soma = (quilo) * (qtd);
    printf("o valor eh %f", soma);

    return (0);
}
